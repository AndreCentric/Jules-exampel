meta {
  name: Create Booking
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/bookings
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "carId": "VW-ID3-001",
    "userId": "user_123"
  }
}

settings {
  encodeUrl: false
}
